{
  "name": "jevibe-check",
  "version": "0.1.3",
  "scripts": {
    "test": "node --test tests/*.test.js && playwright test",
    "test:unit": "node --test tests/*.test.js"
  },
  "keywords": [],
  "license": "MIT",
  "description": "Tone labels for social posts and drafts using TypeSafe's Jev API.",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  },
  "private": true,
  "type": "module"
}
